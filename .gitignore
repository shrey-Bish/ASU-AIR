# Local secrets
.env
.env.*

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Decks / source documents (large binaries, kept local)
*.ppt
*.pptx
*.key
*.doc
*.docx
*.pdf

# Test decks (downloaded, not ours to redistribute) and generated output
decks/
out/

# The two demo decks ARE committed so the team can pull them (see NEXT_STEPS.md).
# ASU course material — remove before the repo goes anywhere public-facing.
!demo-decks/
!demo-decks/*.pptx

# Working data: test decks, conversions, demo output, challenge materials.
# Not ours to redistribute, and large.
data/
